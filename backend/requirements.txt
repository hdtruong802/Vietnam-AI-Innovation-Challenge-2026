# Backend dependencies
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
httpx>=0.25.0

# LLM Gateway (provider-neutral, optional): chi duoc import khi AI_API_KEY
# duoc cau hinh (xem app/services/llm/gateway.py, D-011). Thieu package nay
# van chay duoc, gateway se fallback deterministic offline.
openai>=1.40.0

# Testing and development
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0
