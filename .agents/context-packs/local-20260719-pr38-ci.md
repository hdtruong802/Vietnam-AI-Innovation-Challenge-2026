# Context Pack — local-20260719-pr38-ci

## Identity

- Task ID: `local-20260719-pr38-ci` | GitHub Issue: `PR #38`
- Owner tạm thời: Codex
- Mode hiện tại: `implement`
- Base ref / commit: `origin/dev` / `20f0dca742289a3e11bd00339c34e5fe865ce2ab`
- Branch / worktree: `fix/local-20260719-pr38-ci` / `.worktrees/fix-pr38-ci`
- AI Log member / tool binding / readiness: `hdtruong802 / codex manual / cần onboarding trước commit`

## Mục tiêu và ranh giới

- Mục tiêu: Sửa hai lỗi CI của PR #38: Black backend và policy AI Log historical range.
- Non-goals: Không đổi API, runtime backend, frontend, deploy hoặc viết lại Git history.
- Acceptance criteria: `black --check .`, `flake8 .`, `pytest -q`, repository guard và kiểm tra range `origin/main...HEAD` đều pass; các commit legacy thiếu evidence chỉ được miễn có lý do rõ ràng.
- Constraints: Giữ enforcement AI Log cho commit mới; chỉ thêm ngoại lệ cho 10 commit đã có trong PR #38; không chứa prompt/session/secret.
- Stop condition / blocker cần hỏi peer: Dừng nếu `origin/dev` thay đổi trong lúc làm hoặc CI yêu cầu exemption ngoài 10 commit đã xác minh.

## Scope đã claim

- Files/areas được phép chạm: `backend/**/*.py`, `backend/tests/**/*.py`, `evidence/ai-log/policy.json`, `docs/ai/DECISIONS.md`, Context Pack.
- API, schema hoặc contract liên quan: Chỉ CI/history policy; không đổi public API.
- Không được chạm: frontend, Cloud Run/Vercel configuration, backend runtime behavior.
- Risk: `shared`
- Decision Log liên quan: `D-027`.

## Context được chọn lọc

| Nguồn | File / line / ref | Lý do cần đọc |
| --- | --- | --- |
| CI workflow | `.github/workflows/repository-guard.yml` | Xác nhận jobs gây fail và range policy. |
| Guard | `scripts/ci/validate_repo.py::validate_ai_log_history` | Chỉ exemptions tại head policy được dùng cho history. |
| Policy | `evidence/ai-log/policy.json` | Bổ sung ngoại lệ có audit trail. |
| CI log | Actions run `29668059892` | Xác nhận 8 file Black và 10 commit thiếu trailer. |

## Dependencies và resource claim

- Depends on / blocked by: User đã xác nhận sửa hai lỗi; remote `origin/dev` phải giữ nguyên trong task.
- Shared resource: CI policy / backend formatting.
- Claim owner + thời hạn: Codex đến khi handoff của PR #38.
- Cách thông báo peer và điều kiện release: Handoff nêu rõ 10 SHA exempt; release khi checks local pass.

## Kiểm chứng và handoff

- Commands / manual checks: `black --check .`, `flake8 .`, `pytest -q`, `python scripts/ci/validate_repo.py`, range guard và `git diff --check`.
- Demo impact và rollback: Revert commit này để bỏ exemptions và formatter-only diff.
- Evidence / kết quả: Chờ sau verify.
- AI-Log ID + capture status: Chờ commit.
- Files, API và resources đã chạm: Chờ sau implement.
- Claims đã release: Chờ handoff.
- Việc tiếp theo hoặc peer có thể tiếp nhận: Push branch sửa và rerun PR #38 checks.
